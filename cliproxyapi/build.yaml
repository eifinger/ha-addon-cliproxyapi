build_from:
  aarch64: ghcr.io/home-assistant/aarch64-base:3.19
  amd64: ghcr.io/home-assistant/amd64-base:3.19
# Note: `args:` in build.yaml is deprecated. The CLIPROXYAPI_VERSION default
# lives in the Dockerfile; override it via `--build-arg` or by editing the
# Dockerfile.
